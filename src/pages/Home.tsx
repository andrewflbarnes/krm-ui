import { Typography } from "@suid/material";

export default function Home() {
  return (
    <div style={{
      display: "grid",
      height: "100%",
      width: "100%",
      "place-items": "center",
    }}>
      <Typography variant="h1">[ K ]</Typography>
    </div>
  )
}
